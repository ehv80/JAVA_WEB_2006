<!DOCTYPE form-validation PUBLIC
    "-//Apache Struts//XWork Validator 1.0.2//EN"
    "http://struts.apache.org/dtds/xwork-validator-1.0.2.dtd">

<!-- Antes se hacía así para la versión 1 de Struts  
<!DOCTYPE form-validation PUBLIC
    "-//Apache Software Foundation//DTD Commons Validator Rules Configuration 1.1.3//EN"
    "http://jakarta.apache.org/commons/dtds/validator-1.1.3.dtd">
-->

<!-- Elemento raíz -->
<form-validation>

<!-- Conjunto de formularios -->
    <formset>
    <!-- Agregamos nuestro formulario al conjunto -->
        <form name="proyectoForm">

            <!-- Primer Campo a validar -->
            <field property="nombre" depends="required">
                <arg position="0" key="proyecto.nombre"/>
            </field>

            <!-- Segundo Campo a validar -->
            <field property="fecha" depends="date">
                <arg position="0" key="proyecto.fecha"/>
                <var>
                    <var-name>datePattern</var-name>
                    <var-value>MM-dd-yyyy</var-value>
                </var>
            </field>
        </form>

        <!-- Fornulario del ejemplo del validador validwhen con el ejemplo de las cuotas -->
        <form name="cuotasForm">
            <!-- Validamos el campo cantCuotas con tres validadores -->
            <field property="cantCuotas" depends="validwhen, integer, intRange">
                <!-- Argumento para los mensajes de error, sirve tanto para integer como para intRange -->
                <arg key="form.cantCuotas" position="0"/>

                <!-- Argumento para el mensaje de error del validador intRange -->
                <arg name="intRange" key="${var:min}" resource="false" position="1"/>
                <arg name="intRange" key="${var:max}" resource="false" position="2"/>

                <!-- Mensaje a mostrar si falla el validador validwhen -->
                <msg name="validwhen" key="form.cantCuotas.msg"/>

                <!-- Configuración de validwhen -->
                <var>
                    <var-name>test</var-name>
                    <var-value>((cuotas == null) or (*this* != null))</var-value>
                </var>

                <!-- Variables para configurar el validador intRange -->
                <var>
                    <var-name>min</var-name>
                    <var-value>2</var-value>
                </var>
                <var>
                    <var-name>max</var-name>
                    <var-value>12</var-value>
                </var>
            </field>
        </form>
    </formset>
</form-validation>
